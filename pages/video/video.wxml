<view class="video-list" wx:for="{{ videoList }}" wx:key="user">
  <view class="video-title">标题：{{ item.title }}</view>
  <view class="video-time">时间：{{ item.createTime }}</view>
  <video src="{{ item.src }}" danmu-list="{{ item.danmuList }}" objectFit="fill" enable-danmu></video>
</view>